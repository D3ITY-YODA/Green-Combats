apiVersion: v1
kind: Secret
metadata:
  name: greencompass-secrets
  namespace: greencompass
type: Opaque
stringData:
  # IMPORTANT: Replace these values. Never commit real secrets.
  GC_DATABASE_URL: "postgres://user:password@postgres-service:5432/greencompass?sslmode=require"
  GC_AUTH_SECRET: "CHANGE_ME_TO_A_RANDOM_64_CHARACTER_STRING"
