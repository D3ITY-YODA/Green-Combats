apiVersion: v1
kind: Service
metadata:
  name: greencompass-api
  namespace: greencompass
  labels:
    app: greencompass-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: greencompass-api
